CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
PROJECT(helloworld)
SET(OUTPUT_ELF "${CMAKE_PROJECT_NAME}")

IF(DEFINED TARGET)
  SET(OUTPUT_ELF "${OUTPUT_ELF}-${TARGET}")
ENDIF()

SET(SOURCE_FILES helloworld.c)

ADD_EXECUTABLE(${OUTPUT_ELF} ${SOURCE_FILES})
